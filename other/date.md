
# 为什么你用了十年 date，却从来不知道它还是个计算器？

如果你问一个 Linux 用户"date 是干什么的"，十个里有九个会说："看当前时间的啊，还能干什么。" 剩下那一个会说："还能格式化输出文件名后缀。"

但实际上，date 真正的价值藏在 `-d` 参数后面——它是一个时间计算引擎。它能帮你算出"下周一""三天前""本月最后一天"，甚至能直接把日志里的时间戳转成你认识的格式。然而大部分人就把它当成一个不会走的钟在用。

这篇文章不列参数表，而是从真实场景出发，把 date 身上被忽略的能力和最容易踩的坑一次讲清楚。

---

## ⚠️ 先排雷：Linux 和 macOS 的 date 根本不是同一个命令

在讲任何能力之前，必须先讲这个坑——因为如果你不搞清楚这一点，下面所有的例子在你电脑上可能一个都跑不通。

Linux 用的是 **GNU date**，macOS 自带的是 **BSD date**，两者最关键的差异在时间计算：

```bash
# Linux（GNU date）：用 -d 做时间运算
date -d "1 day ago" "+%F"      # 昨天
date -d "next Monday" "+%F"    # 下周一

# macOS（BSD date）：用 -v 做时间运算
date -v-1d "+%F"               # 昨天
date -v+1d "+%F"               # 明天
date -v+monday "+%F"           # 下周一
```

> 💡 如果你需要在 macOS 上用 GNU date 的语法，装 coreutils：`brew install coreutils`，然后使用 `gdate` 代替 `date`。如果要写跨平台脚本，要么做条件判断，要么老老实实用 Python/Perl 的时间库。

这篇文章以下所有例子默认 Linux（GNU date），遇到 macOS 差异的地方会单独标注。

---

## date 到底是什么，以及为什么 `+` 开头的字符串不是随便写的

date 的核心逻辑只有一句话：**把一个时间点，按你指定的格式，输出成字符串。** 如果不给 `-d`，那个时间点就是"现在"；如果给了 `-d`，那个时间点就是你说的任意时间。

它跟 `ls`、`cat` 这些命令有个本质区别——date **几乎从来不用于"看"什么信息**，它主要用于 **"生成"信息**。你运行 `date` 的目的不是看现在几点（看桌面右上角不就行了），而是要把一个时间字符串嵌进文件名、日志、脚本输出里。

`+` 后面的 `%Y`、`%m` 这些叫 **格式控制符**，`+` 本身的作用是告诉 date："后面这一串是格式，不是别的参数。" 如果你忘了写 `+`，date 会按它的默认格式输出，而不是你期望的那一长串字符：

```bash
# ❌ 忘了写 +
date "%Y-%m-%d"        # 输出：date: invalid date '%Y-%m-%d'

# ✅ 正确
date "+%Y-%m-%d"       # 输出：2025-07-13
```

---

## 核心能力逐层拆解

### 第 1 层：格式化输出 —— 你最常用的部分

```
date "+%Y-%m-%d %H:%M:%S"     # 2025-07-13 14:30:00
date "+%F %T"                  # 2025-07-13 14:30:00（%F 和 %T 是上面的简写）
date "+%s"                     # 1752460200 —— Unix 时间戳
date "+%Y%m%d_%H%M%S"         # 20250713_143000 —— 文件命名的黄金格式
```

常用格式符速查，不需要背，记住有这张表就行：

| 格式符 | 含义 | 示例 | 记忆技巧 |
|--------|------|------|----------|
| `%Y` | 四位年份 | 2025 | Year |
| `%m` | 两位月份 (01-12) | 07 | Month |
| `%d` | 两位日期 (01-31) | 13 | Day |
| `%H` | 24小时制小时 (00-23) | 14 | Hour |
| `%M` | 分钟 (00-59) | 30 | Minute |
| `%S` | 秒 (00-59) | 00 | Second |
| `%s` | Unix 时间戳 | 1752460200 | seconds since epoch |
| `%F` | `%Y-%m-%d` 的简写 | 2025-07-13 | Format 全日期 |
| `%T` | `%H:%M:%S` 的简写 | 14:30:00 | Time |
| `%A` | 完整星期名 | Sunday | |
| `%u` | 星期几 (1=周一, 7=周日) | 7 | |
| `%Z` | 时区名 | CST | Zone |
| `%z` | 时区偏移 | +0800 | |

> 💡 小写 `%s` 和大写 `%S` 长得像，但含义完全不同。`%s` 是时间戳（一个很长的大整数），`%S` 是秒（00-59）。搞反了不会报错，但输出会很奇怪。

---

### 第 2 层：时间计算 —— date 真正的杀手能力

这是最被低估的部分。很多人遇到"三天前是几号""下周一是什么日子"这种问题，下意识打开日历 app 点来点去，根本没想过一行命令就能解决。

`-d` 参数（GNU date）接受的是一段**自然语言描述的日期字符串**，date 会解析它并输出对应的时间：

```bash
# 基础偏移
date -d "1 day ago" "+%F"           # 昨天
date -d "yesterday" "+%F"           # 同上，更自然
date -d "1 day" "+%F"               # 明天
date -d "tomorrow" "+%F"            # 同上
date -d "3 days ago" "+%F"          # 三天前
date -d "2 weeks" "+%F"             # 两周后
date -d "1 month ago" "+%F"         # 一个月前
date -d "1 year" "+%F"              # 一年后

# 组合偏移
date -d "2 weeks 3 days ago" "+%F"  # 两周零三天前

# 小时/分钟/秒
date -d "3 hours ago" "+%T"         # 三小时前是几点几分
date -d "30 minutes" "+%T"          # 30 分钟后
```

**星期计算**也是开箱即用的：

```bash
date -d "next Monday" "+%F"         # 下周一
date -d "last Friday" "+%F"         # 上周五
date -d "this Wednesday" "+%F"      # 本周三（如果今天是周四，就是昨天）
```

macOS 用户对应写法：

```bash
# macOS BSD date：-v 后面接 [+-]数字[单位]
date -v-1d "+%F"       # 昨天
date -v+2w "+%F"       # 两周后
date -v-3H "+%T"       # 3 小时前
date -v+monday "+%F"   # 下周一（注意是小写）
```

> ⚠️ 第二个坑：月末加减月份的行为
>
> ```bash
> # 如果今天是 1 月 31 日
> date -d "1 month" "+%F"    # 输出可能是 2025-03-03，不是 2 月 28 日！
> ```
>
> 大部分实现会先加月份到 2 月 31 日，发现不存在这个日期，自动修正到 3 月 3 日。如果你需要严格的"加一个月 = 同一天或月末最后一天"，date 自己做不到，需要用更高级的时间库。

---

### 第 3 层：巧用时间锚点，算出各种边界日期

`-d` 的真正威力不只在于加减偏移，而在于它可以指定一个**锚点日期**，然后从那个锚点出发做运算：

```bash
# 本月第一天
date -d "$(date +%Y-%m-01)" "+%F"

# 本月最后一天：下个月第一天的前一天
date -d "$(date +%Y-%m-01) +1 month -1 day" "+%F"

# 本周一
date -d "last Monday" "+%F"      # 如果今天是周一，会返回上周一
date -d "Monday" "+%F"           # 如果今天是周一，返回今天
# 更可靠的做法：
date -d "$(date -d "this Monday" "+%F")" "+%F" 2>/dev/null || date -v+monday "+%F"

# 下个月最后一天
date -d "$(date +%Y-%m-01) +2 months -1 day" "+%F"

# 今年元旦
date -d "$(date +%Y)-01-01" "+%F"

# 某个具体日期的前一天
date -d "2025-10-01 -1 day" "+%F"   # 2025-09-30

# 某个日期的星期几
date -d "2025-07-13" "+%A"          # Sunday
```

这里最关键的技术是：**利用 `$(date +%Y-%m-01)` 构造出"本月第一天"这个锚点，然后在上面做加减法。** 这是 date 最优雅的用法之一。

> ⚠️ **第三个坑：`+1 month` 和 `+30 days` 不一样。**
>
> ```bash
> # 从 3 月 1 日开始
> date -d "2025-03-01 +1 month" "+%F"   # 2025-04-01
> date -d "2025-03-01 +30 days" "+%F"   # 2025-03-31
> ```
>
> 如果你要的是"日历意义上的下个月同一天"，用 month；如果你要的是"严格加 30 天"，用 days。两者的语义不同，在脚本里搞混会导致边界 bug。

---

### 第 4 层：时间戳互转 —— 日志排查的瑞士军刀

日志里经常出现 Unix 时间戳（一个 10 位或 13 位的数字），肉眼根本看不懂。date 可以一键转换：

```bash
# 时间戳 → 可读时间
date -d @1752460200 "+%F %T"                    # 2025-07-13 20:30:00
date -d @1752460200 -u "+%F %T %Z"              # 强制 UTC 显示

# 可读时间 → 时间戳
date -d "2025-07-13 20:30:00" "+%s"             # 1752460200

# 直接读某个时间戳是星期几
date -d @1752460200 "+%A"                       # Sunday
```

注意 `@` 前缀：它是告诉 date "后面这个数字是 Unix 时间戳，不是别的奇怪格式。" 不要省略 `@`。

macOS 用户：

```bash
# BSD date 用 -r 参数读时间戳
date -r 1752460200 "+%F %T"
```

> 💡 毫秒时间戳（13 位）不能直接丢给 date——先除以 1000 去掉后三位。

---

### 第 5 层：时区 —— 一行命令对比全球时间

```bash
# 指定时区输出
TZ='Asia/Shanghai' date "+%F %T %Z"              # 上海
TZ='America/New_York' date "+%F %T %Z"           # 纽约
TZ='Europe/London' date "+%F %T %Z"              # 伦敦
TZ='UTC' date "+%F %T %Z"                        # UTC

# 把某个时间换算到另一个时区
TZ='America/Los_Angeles' date -d "2025-07-13 20:00:00 CST" "+%F %T %Z"
```

这对做跨国业务、排查海外服务器日志非常有用——你不用去网上找时区换算器。

> 💡 `TZ` 是一个环境变量前缀，只影响这一行命令，不会修改系统时区。开箱即用，不需要 sudo。

---

### 第 6 层：设置系统时间（知道就行，别轻易用）

```bash
date -s "2025-07-13 14:30:00"   # 设置系统时间（需要 root）
date -s "14:30:00"               # 只改时间，不改日期
hwclock -w                       # 把系统时间写入硬件时钟（否则重启丢失）
```

实际生产环境几乎不会手动设时间，全靠 NTP 自动同步。但离线环境或虚拟机时钟漂移严重时，这条命令就是救命稻草。

---

## 场景驱动：date 在实战中的五个高频用法

### 1. 定时脚本里的结构化日志

```bash
#!/bin/bash
echo "[$(date '+%F %T')] 清理任务开始"
# ... do work ...
echo "[$(date '+%F %T')] 清理完成，共删除 $count 个文件"
```

时间戳前缀让日志可查询、可排序：

```bash
# 日志长这样
[2025-07-13 02:30:00] 清理任务开始
[2025-07-13 02:30:05] 清理完成，共删除 142 个文件
```

---

### 2. 备份文件自动带日期后缀

```bash
# 每天生成独立备份
cp config.json config.json.bak.$(date +%Y%m%d)
mysqldump -u root mydb > mydb_$(date +%Y%m%d_%H%M%S).sql

# 清理 7 天前的备份
find /backup -name "*.bak.2025*" -mtime +7 -print   # 先确认
find /backup -name "*.bak.2025*" -mtime +7 -delete  # 确认无误后执行
```

> 💡 `%Y%m%d_%H%M%S` 这个格式按字典序排列就是按时间排序，非常方便 `ls` 后肉眼浏览。

---

### 3. 日志排查：把时间戳一行看懂

```bash
# Nginx 日志里的时间戳看不懂？
tail -5 /var/log/nginx/access.log | awk '{print $1}' | while read ts; do
  date -d "@$ts" "+%F %T"
done
```

---

### 4. 计算两个日期的天数差

```bash
diff=$(( ($(date -d "2025-12-31" +%s) - $(date -d "2025-01-01" +%s)) / 86400 ))
echo "相差 $diff 天"    # 364
```

原理：把两个日期都转成时间戳（秒数），相减后除以 86400（一天的秒数）。

---

### 5. 生成按日期命名的目录树

```bash
# 按日期组织日志
year=$(date +%Y)
month=$(date +%m)
day=$(date +%d)
log_dir="/var/log/app/$year/$month/$day"
mkdir -p "$log_dir"
```

每个脚本里都有人写过类似的东西。

---

## 新手踩坑总结

- **坑一：macOS vs Linux 语法不兼容。** GNU date 用 `-d`，BSD date 用 `-v`。跨平台脚本要么做条件判断，要么用 Python。
- **坑二：忘记写 `+` 号。** `date "%Y"` 会报错，正确是 `date "+%Y"`。
- **坑三：月末加减月份的行为不符合直觉。** `1月31日 +1 month` 可能得到 3 月 3 日。需要严格语义时换工具。
- **坑四：`%s` 和 `%S` 搞混。** 一个是时间戳（大整数），一个是秒（00-59），输出完全不一样但不报错。
- **坑五：时间戳忘了加 `@` 前缀。** `date -d 1752460200` 会被当成奇怪的日期格式解析，正确是 `date -d @1752460200`。
- **坑六：`+1 month` ≠ `+30 days`。** 前者是日历月，后者是严格天数，脚本里搞混会出边界 bug。
- **坑七：`date -d "Monday"` 的行为取决于今天是星期几。** 今天是周一就返回今天，其他天返回下一个周一。不够可靠时，用具体日期做锚点。

---

## 什么时候换工具

date 不是万能的。以下场景它做不好，不要硬撑：

| 场景 | 为什么 date 不行 | 推荐替代 |
|------|-----------------|---------|
| 严格日期运算（含闰年、月末） | date 的 month 运算会溢出修正 | Python `datetime` / `dateutils` |
| 两个日期之间的工作日计算 | date 不支持"跳过周末" | Python `numpy.busday_count` 或 Excel |
| 毫秒/微秒精度 | date 精度到秒 | `date +%s%N`（纳秒，GNU only），高精度用 Python `time.time_ns()` |
| 批量处理日志中的各种格式的时间 | date 只接受特定格式 | Python `dateutil.parser.parse()` |
| 跨平台可靠使用 | Linux/macOS 语法不兼容 | Python `datetime`（真正跨平台） |

> 一句话：**日常命令行随手算一下，用 date；写进生产脚本的时间逻辑，用 Python。**

---

## 最后

date 被低估的根本原因，是太多人把它当成"看时间的"，而没意识到它其实是一个**命令行里的时间计算引擎**。

你平时用 date 是只敲 `date` 回车看一眼，还是会接格式化字符串？如果你到现在还没用过 `-d`，下一次需要算"三天后是几号"的时候，试试用 date 而不是打开日历——那种"一行命令解决"的快感，就是学 Linux 的乐趣所在。
